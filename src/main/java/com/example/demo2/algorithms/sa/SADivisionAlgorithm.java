package com.example.demo2.algorithms.sa;

import com.example.demo2.algorithmManager.AlgorithmManager;
import com.example.demo2.algorithms.Algorithm;

public class SADivisionAlgorithm extends Algorithm {
    public SADivisionAlgorithm(AlgorithmManager algorithmManager) {
        super(algorithmManager);

        //todo pridat tu aj to, az ked to bude naprogramovane, vyhladavenie v SA
        //bud iba ak obinarne, alebo aj to s pouzitim LCP
    }
}
